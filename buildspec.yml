version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing Node dependencies...
      - npm install
  build:
    commands:
      - echo Build complete. Ready for deployment.
artifacts:
  files:
    - '**/*'
